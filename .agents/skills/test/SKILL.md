---
name: test
description: |
  Acts as an Automated Test Developer (SDET) expert in C# for GrupoXpert.
  Organizes tests following the `tests/` folder of the Project's General Structure.
author: German Alvarez
version: 1.3.0
---

# Goal
Validate GrupoXpert's logic through automated tests organized by project, using 100% SPANISH for test scenario naming while maintaining English for structural components.

# Test Structure
Tests must be located in folders parallel to the source:
- `tests/GrupoXpert.Domain.Tests/`: Business logic and invariants.
- `tests/GrupoXpert.Application.Tests/`: MediatR handlers and validations.
- `tests/GrupoXpert.Infrastructure.Tests/`: Real integrations or complex mocks.
- `tests/GrupoXpert.WebApi.Tests/`: Full endpoint integration.

# Instructions
- Method names: `Action_Scenario_ExpectedResult` in Spanish (e.g., `Crear_CuandoFechaEsHoy_DebeLanzarExcepcion`).
- Use **NSubstitute** to mock project interfaces (e.g., `IUsuarioRepository`).
- Apply **FluentAssertions** (`.Should().Be()`).

# Example
```csharp
// tests/GrupoXpert.Domain.Tests/SolicitudTests.cs
[Fact]
public void Crear_CuandoFechaLimiteEsHoy_DebeLanzarExcepcion() { ... }
```

# Constraints
- 🚫 **NO ENGLISH IN UBIQUITOUS LANGUAGE**: Entities, DTOs, test methods, and variables (`estudiante`, `solicitud`) MUST be in SPANISH.
- ✅ **YES TO STRUCTURAL ENGLISH**: Test project names (`GrupoXpert.UnitTests`), mocking libraries, and test class suffixes (`SolicitudTests.cs` instead of `Pruebas.cs`) MUST respect English architecture conventions.
- ✅ **Isolation**: Domain tests must not use infrastructure mocks.

<!-- Generated by Skill Creator Ultra v1.3.0 -->
